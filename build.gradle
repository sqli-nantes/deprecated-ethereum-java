allprojects {
    repositories {
        jcenter()
    }
}

buildscript {
    repositories {
        jcenter()
	}
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
